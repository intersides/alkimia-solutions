{
  "name": "backend-server",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "dependencies": {
    "@workspace/common": "*",
    "@workspace/node": "*"
  },
  "scripts": {
    "development": "NODE_ENV=development nodemon --watch . --watch ../../libs -e js,json main.js",
    "production": "NODE_ENV=production node main.js"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
